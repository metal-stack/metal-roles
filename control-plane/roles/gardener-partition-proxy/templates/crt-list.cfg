{% for conf in gardener_shooted_seed.proxy_servers %}
/etc/ssl/{{ conf.name }}/combined.pem {% if conf.mtls | default(true) %}[ca-file /etc/ssl/{{ conf.name }}/ca.pem verify required] {% endif %}{{ conf.ingress_domain }}
{% endfor %}
