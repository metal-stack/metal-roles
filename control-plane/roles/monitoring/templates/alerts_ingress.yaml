apiVersion: v1
kind: Secret
metadata:
  name: alerts-basic-auth
  namespace: monitoring
type: Opaque
data:
  auth: 
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monitoring-ingress-alerts
  annotations:
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: alerts-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required - alertmanager'
    cert-manager.io/cluster-issuer: letsencrypt-prod
    fluentbit.io/parser: nginx
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - alerts.{{ metal_control_plane_ingress_dns }}
      secretName: monitoring-ingress-tls-alerts
  rules:
  - host: alerts.{{ metal_control_plane_ingress_dns }}
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: prometheus-alertmanager
            port:
              number: 9093
