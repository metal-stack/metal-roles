apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: alertlogger
  labels:
    app: alertlogger
spec:
  serviceName: alertlogger
  replicas: 1
  selector:
    matchLabels:
      app: alertlogger
  template:
    metadata:
      labels:
        app: alertlogger
    spec:
      automountServiceAccountToken: false
      containers:
      - name: alertlogger
        image: docker.io/mwennrich/alertlogger:v0.9.0
        ports:
        - containerPort: 5001
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
