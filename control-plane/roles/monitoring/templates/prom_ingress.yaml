apiVersion: v1
kind: Secret
metadata:
  name: prom-basic-auth
  namespace: monitoring
type: Opaque
data:
  auth: 
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monitoring-ingress-prom
  annotations:
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: prom-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required - prometheus'
    cert-manager.io/cluster-issuer: letsencrypt-prod
    fluentbit.io/parser: nginx
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - prom.{{ metal_control_plane_ingress_dns }}
      secretName: monitoring-ingress-tls-prom
  rules:
  - host: prom.{{ metal_control_plane_ingress_dns }}
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: prometheus-prometheus
            port:
              number: 9090
