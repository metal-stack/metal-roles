---
skip_list:
  - galaxy[no-changelog]
  - yaml[line-length]

exclude_paths:
  - .cache/
  # these are template results, so they do not need formatting:
  - partition/roles/sonic/test/data/**/metal.yaml
